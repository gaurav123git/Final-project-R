package com.velocity.insurance.add.service;

import com.velocity.insurance.add.DriverDetails;

public interface DriverDetailsService {
	
	public DriverDetails saveDriverDetails(DriverDetails driverDetails);

}
